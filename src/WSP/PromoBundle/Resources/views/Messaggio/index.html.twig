{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Messaggio list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Subject</th>
                <th>Body</th>
                <th>Destinatari</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('messaggio_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.subject }}</td>
                <td>{{ entity.body }}</td>
                <td>{{ entity.destinatari }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('messaggio_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('messaggio_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('messaggio_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
