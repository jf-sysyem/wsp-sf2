{% block extends %}
{{ "{% extends '::base.html.twig' %}" }}
{{ "{% if form_layout %}" }}
{{ "    {% form_theme form form_layout %}" }}
{{ "{% endif %}" }}

{% endblock extends %}

{% block body %}
{{ "{% block content %}" }}
<div class="page-header">
    <div class="icon">
        <span class="ico-cog"></span>
    </div>
    <h1>{{ entity }} <small>Nuovo</small></h1>
</div>
{{ '{{ form_start(form) }}' }}
    <div class="row-fluid">
        <div class="span6">
            <div class="block">
                <div class="head">                                
                    <h2>{{ entity }}</h2>
                </div>                                        
                <div class="data-fluid">
                {{ '{{ form_rest(form) }}' }}
                </div>                                        
            </div>
        </div>
    </div>
{{ '{{ form_end(form) }}' }}
{% set hide_edit, hide_delete = true, true %}
{% include 'crud/views/others/record_actions.html.twig.twig' %}
{{ "{% endblock %}" }}
{% endblock body %}
