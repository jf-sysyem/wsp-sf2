<button class="btn yellow btn-xs" onclick="window.location='{{ "{{ path('" ~ route_name_prefix ~ "') }}" }}';">Torna indietro</button></a>
<a href="{{ "{{ path('" ~ route_name_prefix ~ "') }}" }}">
    <span class="label label-inverse">Torna indietro</span>
</a>

{% if ('edit' in actions) and (not hide_edit) %}
    {%- if has_slug %}
            <button class="btn yellow btn-xs" onclick="window.location='{{ "{{ path('" ~ route_name_prefix ~ "_edit', { 'slug': entity.slug }) }}" }}';">Modifica</button></a>
    {%- else %}
            <button class="btn yellow btn-xs" onclick="window.location='{{ "{{ path('" ~ route_name_prefix ~ "_edit', { 'id': entity.id }) }}" }}';">Modifica</button></a>
    {%- endif %}
{% endif %}
{% if ('delete' in actions) and (not hide_delete) %}
    {{ '{{ form(delete_form) }}' }}
{% endif %}
