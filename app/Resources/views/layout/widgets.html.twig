{% if widgets is defined %}
<div class="row-fluid">
    <div class="span12">
        <div class="widgets">
            {% for widget in widgets %}
                {% render(controller(widget.render, widget.params)) %}
            {% endfor %}
        </div>
    </div>
</div>
{% endif %}